# L1 常见题型复习与练习

## 一、写代码容易踩的坑

> 做题时如果程序报错了，不要慌！先自己运行看看结果，然后对照下面的清单，一项一项排查，就像玩“找不同”游戏一样。

### （一）答案错了（WA）

这种情况是程序能跑起来，但是输出的结果和题目要求不一样。

1. **变量的“箱子”装错了类型**
   比如题目要求小数，你却用了整数（`int`）的箱子。检查一下每个变量的类型对不对。

2. **变量忘了初始化（给初始值）**
   就像新买的计数器，一开始应该是 `0`，否则里面的数字是乱七八糟的。
   **记住**：`sum`（求和）要从 `0` 开始，`ans`（求乘积）要从 `1` 开始，`cnt`（计数）要从 `0` 开始。

3. **循环的条件写错了**
   检查一下 `for` 循环的起点、终点、步长（每次增加多少）。比如是从 `1` 到 `n`，还是从 `0` 到 `n-1`？边界是 `<=` 还是 `<`？

4. **输入的顺序不对**
   题目说先输入年龄再输入分数，你却先输分数再输年龄。一定要**仔细读题目里的“输入描述”**，按顺序输入。

5. **代码放错了地方**
   - 应该重复执行（在循环里）的代码，写到了循环外面。
   - 应该只执行一次（在循环外）的代码，写到了循环里面。
   想一想：这行代码是要做很多遍，还是只做一遍？

6. **符号写错了**
   最容易犯的：判断相等要用 `==`，但写成了 `=`（赋值）。
   比如 `if (a == 5)` 是正确的，`if (a = 5)` 就会出错。

7. **除法的类型**
    整数相除的到的是整数，如果想要精确的数值，需要改成`double`类型

8. **整个思路不对**
   如果上面都没问题，但答案还是错，那可能是解题思路错了。拿出草稿纸，重新读题，一步一步画出来或写下来，想想还有没有更好的方法。

### （二）编译错误（CE）

编译错误就是程序根本跑不起来，像句子不通顺一样。通常是代码里出现了“语法毛病”。

1. **变量没有定义就用了**
   先定义（比如 `int a;`），再使用。不能直接写 `a = 5;` 而前面没有 `int a;`。

2. **变量名字写错了**
   前面定义了 `sum`，后面写成了 `sun`。仔细检查拼写，大小写也要一致。

3. **漏掉了分号、括号等符号**
   每一句代码结尾要有分号 `;`，大括号 `{}` 要成对出现。
   **好习惯**：写代码时保持整洁的缩进和格式，能减少这种错误。

4. **不小心打出了中文字符**
   C++ 里所有的标点符号都必须是**英文**的。比如 `;` 是英文分号，`；` 是中文分号（会报错）。
   在代码编辑器里，中文字符通常会显示不一样，注意观察。

5. **同一个变量定义了两次**
   比如写了 `int a;` 然后又写一次 `int a;`。每个变量只需要定义一次。

### （三）运行错误（RE）

程序跑着跑着突然“崩溃”了，就像游戏突然闪退。

1. **除以 0**
   如果让计算机做 `a / b` 而 `b` 可能是 0（比如忘记给 b 输入值），程序就会崩溃。
   **提醒**：除法前确保除数不是 0。

2. **死循环 + 不停输出**
   如果 `while(1)` 或者 `for` 循环条件永远为真，里面又有 `cout`，就会无限输出刷屏，最后导致运行错误（而不是超时）。
   **解决方法**：检查循环条件，确保它能正常结束。

3. **数组开得太大**
   数组就像一排连续的“柜子”。如果你开了 `int a[100000000]`（1 亿个），电脑内存放不下，程序就会崩溃。
   **一般建议**：数组长度不超过 1000 万（`10^7`），实际题目中通常几万就够了。

### （四）运行超时（TLE）

程序跑得太慢了，超过了题目规定的时间。

- **最常见原因**：循环条件写错了，导致循环次数太多或死循环。
  **怎么办**：在草稿纸上模拟一下循环过程，看看 `i` 的值是怎么变化的，能不能正常走到终点。

---

## 二、常见题型

> **两种题型**：
> - **基础题型**：像积木里的基本块，单独出现，比较简单。
> - **复合题型**：把多个基础题型拼在一起，就像搭积木。L1 中期开始，大部分题目都是这种，需要你会分析题目，把它拆成几个小问题来解决。

---

### 1. 格式化输出 【基础】

- **题目在说什么**：让你按特定的样子输出内容。比如空格在哪，小数点后几位，要不要换行。
- **怎么写**：没有固定写法，按照题目要求用 `cout`写就行。
- **注意**：
  - 空格数量、位置要对。
  - 保留几位小数（`fixed << setprecision(2)`）。
  - 注意换行符 `\n`。
- **练习题**：
  - [输出3个整数的乘积](https://oj.youdao.com/course/83/496/1#/1/7960)
  - [求总票价](https://oj.youdao.com/course/83/496/1#/1/12112)
  - [友好的输出](https://oj.youdao.com/course/83/494/2#/1/7910)

---

### 2. 数学题 【基础】

- **题目在说什么**：把数学应用题变成代码。
- **怎么写**：先自己用数学方法算出式子，再把式子翻译成代码。编程部分通常不难，难的是怎么分析题目。
- **注意**：看清数字是整数还是小数（用 `int` 还是 `double`）。
- **练习题**：
  - [求总票价](https://oj.youdao.com/course/83/496/1#/1/12112)
  - [计算体质指数BMI值](https://oj.youdao.com/course/83/496/1#/1/7961)
  - [买电子听力设备](https://oj.youdao.com/course/83/496/1#/1/12113)

---

### 3. 数位拆分 【基础】

- **题目在说什么**：把一个整数的各个位拆开，比如 `123` → 个位 `3`、十位 `2`、百位 `1`。
- **写法口诀**：
  - `%10` 可以得到最后一位（个位）。
  - `/10` 可以删掉最后一位。
  - 想要拿到哪一位，就先删掉它右边的所有位，再用 `%10`。
- **举例**：拿 `123` 的十位 `2` → 先 `/10` 变成 `12`，再 `%10` 得到 `2`。
- **注意**：在草稿纸上推演一下，要删掉最后几位。
- **代码**（三位数 `a`）：
  ```cpp
  int ge = a % 10;          // 个位
  int shi = a / 10 % 10;    // 十位
  int bai = a / 100;        // 百位
  ```
- **练习题**：
  - [反向输出三位数](https://oj.youdao.com/course/83/494/1#/1/7906)
  - [输出两个整数个位上数字之和](https://oj.youdao.com/course/83/494/2#/1/12426)
  - [计算各位的和](https://oj.youdao.com/course/83/496/2#/1/7952)

---

### 4. 数字的判断 【基础】

- **题目在说什么**：判断一个数字满不满足某个条件，比如是不是偶数、是不是两位数。
- **写法**：用 `if` 搭配 `%`（取余）、比较运算符（`>=`、`<=`）、逻辑运算符（`&&` 表示“并且”，`||` 表示“或者”）。
- **注意**：看清题目是要求“同时成立”还是“其中一个成立”。
- **代码示例**：
  ```cpp
  // 是偶数吗？
  if (a % 2 == 0) { ... }

  // 是奇数吗？
  if (a % 2 != 0) { ... }

  // 最后一位是3吗？
  if (a % 10 == 3) { ... }

  // 是3的倍数，但不是5的倍数
  if (a % 3 == 0 && a % 5 != 0) { ... }

  // 是两位数（10~99）吗？
  if (a >= 10 && a <= 99) { ... }
  ```
- **练习题**：
  - [不一样的计算](https://oj.youdao.com/course/83/509/1#/1/7993)
  - [末位是不是5](https://oj.youdao.com/course/83/509/1#/1/7994)
  - [判断是否为三位数](https://oj.youdao.com/course/83/514/1#/1/8051)
  - [找贝壳](https://oj.youdao.com/course/83/514/1#/1/12908)

---

### 5. 字符的操作 【基础】

- **题目在说什么**：对单个字符进行判断（是不是字母？是不是数字？）或转换（小写变大写）。
- **写法**：字符在计算机里存的是 ASCII 码，可以直接比较和计算。
  比如 `'a'` 是 97，`'A'` 是 65，相差 32。
- **注意**：
  1. 字符可以直接加减数字，比如 `'a' + 1` 得到 `'b'`。
  2. 运算后结果变成整数，如果想输出字符，要转成 `char` 类型：`(char)(c + 32)`。
- **代码示例**：
  ```cpp
  // 大小写转换
  cout << (char)(c - 32);   // 小写转大写
  cout << (char)(c + 32);   // 大写转小写

  // 判断是不是小写字母
  if (c >= 'a' && c <= 'z') { ... }

  // 判断是不是大写字母
  if (c >= 'A' && c <= 'Z') { ... }

  // 判断是不是数字字符
  if (c >= '0' && c <= '9') { ... }

  // 判断字符的 ASCII 码是奇数还是偶数
  if (c % 2 == 0) { ... }   // 偶数
  if (c % 2 != 0) { ... }   // 奇数
  ```
- **练习题**：
  - [debug程序](https://oj.youdao.com/course/83/514/1#/1/18829)
  - [加密游戏](https://oj.youdao.com/course/83/514/1#/1/8054)
  - [字母变换](https://oj.youdao.com/course/83/499/1#/1/12433)
  - [大小写相互转换](https://oj.youdao.com/course/83/509/1#/1/8003)
  - [奇偶ASCII值判断](https://oj.youdao.com/course/83/509/2#/1/8009)

---

### 6. 筛选范围内符合要求的数 【复合】

- **题目在说什么**：从 `A` 到 `B` 这些数字里，挑出所有满足条件的数，一个个输出。
- **写法**：`for` 循环走一遍，里面加上 **数字的判断**。
- **注意**：循环的起点和终点别弄反了，注意边界。
- **代码**：
  ```cpp
  for (int i = 起点; i <= 终点; i++) {
      if (条件) {
          cout << i << ' ';
      }
  }
  cout << '\n';
  ```
- **练习题**：
  - [当for遇到if](https://oj.youdao.com/course/83/515/1#/1/8060)
  - [进阶挑战](https://oj.youdao.com/course/83/515/1#/1/8061)
  - [能被7整除的数字](https://oj.youdao.com/course/83/515/2#/1/8074)

---

### 7. 输入 n 个数 【基础】

- **题目在说什么**：先告诉你有几个数（n），然后把这 n 个数一个一个读进来。
- **写法**：
  1. 定义一个整数 `n`，读入它。
  2. 如果不需要保存所有数，就用一个变量 `t` 每次读入；如果需要保存，就用数组。
  3. `for` 循环 n 次，每次读入一个数。
- **注意**：如果用数组，要根据题目给的 n 的最大值来定义长度（通常 `最大值 + 5`），防止越界。
- **代码**：
  ```cpp
  // 只处理，不保存（比如求和）
  int n, t;
  cin >> n;
  for (int i = 0; i < n; i++) {
      cin >> t;
      // 直接处理 t
  }

  // 需要保存下来（比如逆序输出）
  int n;
  int a[100005];   // 假设 n 最大是 100000
  cin >> n;
  for (int i = 0; i < n; i++) {
      cin >> a[i];
  }
  ```
- **练习题**：无

---

### 8. 计数问题 【复合】

- **题目在说什么**：给你一堆数，问里面**有多少个**符合条件。
- **写法**：
  1. **输入 n 个数**（上面第 7 题）。
  2. 准备一个计数器变量，**一开始是 0**。
  3. 每遇到一个符合条件的数，就让计数器加 1。
  4. 循环结束后，输出计数器的值。
- **注意**：
  - 计数器一定要初始化为 0！
  - 如果需要同时统计好几种，就定义多个计数器。
  - 输出要写在循环外面。
- **代码**：
  ```cpp
  int n, cnt = 0, t;
  cin >> n;
  for (int i = 0; i < n; i++) {
      cin >> t;
      if (符合条件) {
          cnt++;
      }
  }
  cout << cnt;   // 输出有几个
  ```
- **练习题**：
  - [与指定数字相同的数的个数](https://oj.youdao.com/course/83/515/1#/1/8071)
  - [好事不成双](https://oj.youdao.com/course/83/515/1#/1/18833)
  - [不同种类糖果的数量](https://oj.youdao.com/course/83/515/1#/1/12888)
  - [整数的个数](https://oj.youdao.com/course/83/515/2#/1/9036)

---

### 9. 找最值（最大/最小） 【复合】

- **题目在说什么**：给你一堆数，找出里面最大的那个或者最小的那个。
- **写法**：
  1. 输入 n 个数。
  2. 定义一个最大值变量 `max` 和一个最小值变量 `min`。
  3. 先给它们一个“种子值”：最大值变量先设为**可能的最小值减 1**，最小值变量设为**可能的最大值加 1**。
     比如题目说数字在 0~1000 之间，那么 `max = -1`，`min = 1001`。
  4. 遍历每个数，如果发现比当前 `max` 还大，就更新 `max`；如果比当前 `min` 还小，就更新 `min`。
- **注意**：种子值一定要足够“极端”，保证第一个数就能更新它。
- **代码**：
  ```cpp
  int n, a[100005];
  cin >> n;
  for (int i = 0; i < n; i++) {
      cin >> a[i];
  }

  int max = -1;      // 假设数字最小是 0
  for (int i = 0; i < n; i++) {
      if (a[i] > max) max = a[i];
  }

  int min = 1001;    // 假设数字最大是 1000
  for (int i = 0; i < n; i++) {
      if (a[i] < min) min = a[i];
  }
  ```
- **练习题**：
  - [最小的整数](https://oj.youdao.com/course/83/515/1#/1/8063)
  - [挑出最大的西瓜](https://oj.youdao.com/course/83/515/1#/1/8064)

---

### 10. 求和 【复合】

- **题目在说什么**：给你一堆数，把它们全部加起来。
- **写法**：
  1. 输入 n 个数。
  2. 定义 `sum = 0`（一定要是 0！）。
  3. 每读一个数，就加到 `sum` 上。
- **注意**：变种题可能只加符合条件的数，那就在加之前用 `if` 判断一下。
- **代码**：
  ```cpp
  int n, sum = 0;
  int a[100005];
  for (int i = 0; i < n; i++) {
      cin >> a[i];
      sum += a[i];
  }
  ```
- **练习题**：
  - [循环多少次](https://oj.youdao.com/course/83/518/1#/1/19561)
  - [条件求和](https://oj.youdao.com/course/83/518/2#/1/13188)

---

### 11. 求乘积 【复合】

- **题目在说什么**：给你一堆数，把它们全部乘起来。
- **写法**：
  1. 输入 n 个数。
  2. 定义 `ans = 1`（不能是 0，否则乘积永远是 0）。
  3. 每读一个数，就乘到 `ans` 上。
- **注意**：`ans` 必须从 1 开始。
- **代码**：
  ```cpp
  int n, ans = 1;
  int a[100005];
  for (int i = 0; i < n; i++) {
      cin >> a[i];
      ans *= a[i];
  }
  ```
- **练习题**：
  - [求乘积](https://oj.youdao.com/course/83/518/1#/1/13192)

---

### 12. 输入若干个数（以 0 结束） 【复合】

- **题目在说什么**：不知道有几个数，但输入 0 就表示结束了（0 本身不参与计算）。
- **写法**：用 `while` 无限循环，每次读一个数，如果是 0 就 `break` 跳出循环。
- **注意**：这种题一般不会单独出现，而是和计数、求和、找最值等组合在一起。
- **代码模板**：
  ```cpp
  int t;
  while (1) {
      cin >> t;
      if (t == 0) break;
      // 对 t 进行处理，比如计数、累加等
  }
  ```
- **练习题**（都是复合题）：
  - [找出第3个80](https://oj.youdao.com/course/83/517/1#/1/13142)（+计数）
  - [小练习1](https://oj.youdao.com/course/83/517/1#/1/7729)（+求和）
  - [大写字符的数量](https://oj.youdao.com/course/83/518/1#/1/13248)（+计数）
  - [求字母个数](https://oj.youdao.com/course/83/518/2#/1/8044)（+计数）

---

### 13. 逆序输出 【基础】

- **题目在说什么**：把输入的顺序倒过来输出。比如输入 `5 8 3`，输出 `3 8 5`。
- **写法**：
  1. 用数组把所有的数存下来（正向输入）。
  2. 用循环从最后一个元素往前输出。
- **注意**：
  - 数组的索引从 0 开始，最后一个元素的下标是 `长度 - 1`。
  - 逆序循环：`for (int i = n-1; i >= 0; i--)`
- **代码**：
  ```cpp
  // 数组逆序
  int n, a[100005];
  for (int i = 0; i < n; i++) {
      cin >> a[i];
  }
  for (int i = n - 1; i >= 0; i--) {
      cout << a[i] << ' ';
  }

  // 字符串逆序
  string s;
  cin >> s;
  for (int i = s.size() - 1; i >= 0; i--) {
      cout << s[i];
  }
  ```
- **练习题**：
  - [数组代码补全](https://oj.youdao.com/course/83/522/1#/1/20133)
  - [逆序输出所有偶数](https://oj.youdao.com/course/83/521/1#/1/7796)

---

### 14. flag 变量（标记变量） 【复合】

- **题目在说什么**：在一堆数据中**找有没有**符合条件的东西。
  比如：问有没有小写字母？找到了输出“有”，没找到输出“没有”。
- **写法**：
  1. 定义一个 `flag` 变量（就像一面小旗子），一开始让它等于 0（表示“还没找到”）。
  2. 在循环里找，如果找到了，就把 `flag` 改成 1（表示“找到了”）。
  3. 循环结束后，根据 `flag` 的值决定输出什么：`flag == 1` 表示找到了，`flag == 0` 表示没找到。
- **注意**：
  - `flag` 一定要先设为 0。
  - 判断 `flag` 要写在循环外面。
- **代码**：
  ```cpp
  int flag = 0;   // 0 = 没找到，1 = 找到了
  for (int i = 0; i < n; i++) {
      if (条件) {
          flag = 1;
      }
  }

  if (flag == 1) {
      cout << "找到了";
  } else {
      cout << "没找到";
  }
  ```
- **练习题**：
  - [找出与给定数相等的数](https://oj.youdao.com/course/83/517/1#/1/7732)
  - [球员有没有长胖](https://oj.youdao.com/course/83/517/1#/1/7733)
  - [是否有小写字母](https://oj.youdao.com/course/83/518/1#/1/7754)

## 三、解题技巧：
> 在L1的后期，题目复合的内容越来越多，直接动手写很容易两眼一抹黑，思维混乱。为了解决这个问题，同学们可以按照下面的步骤来一步步完成问题。

1. 仔细读题，明确输入的类型和输入的顺序
2. 将题目拆解成多个基础题型的部分
3. 一步步地利用基础题型的写法完成每个部分
4. 将结果串联起来，得到答案

### 举例1：

**题目描述**
阿尔法国的国王想通过信息学竞赛成绩选出整个国家最聪明的公民作为国家领导人，但只有名字中只包含大写字母X、Y和Z的公民才能成为有资格的候选人去参加国王举办的信息学竞赛。现输入一名公民的名字，请你判断他是否是有资格的候选人，如果是请输出Pass，否则输出Not Pass。

**输入描述**
一行，一个不含空白字符的字符串（长度不超过35）

**输出描述**
一行，一个字符串，表示该公民是否是有资格的候选人。

**样例1**
输入
```
XYZ
```
**输出**
```
Pass
```

**样例2**
**输入**
```
xyz
```
**输出**
```
Not Pass
```

**题目分析：**这一题结合了字符串、字符判断和flag变量，是典型的复合题

**解题步骤：**
    1. 查看输入，输入只有一个字符串，所以用到string
    2. 利用for循环检查每一个字符
    3. 在**for循环内**利用if语句进行判断，判断是否出现了`X`、`Y`、`Z`之外的字符
    4. **在if语句中**利用flag变量做标记，一开始为0，找到了标记成1
    5. **在for循环外**判断flag的值，如果是0，表示没找到要求之外的字符，那就是候选人，输出“Pass”，否则输出“Not Pass”

**代码变化：**

`第一步：输入`
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    cin >> s;
    return 0;
}
```

`第二步：循环`
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    cin >> s;
    for (int i = 0;i < s.size(); i++) {

    }
    return 0;
}
```

`第三步：if判断`
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    cin >> s;
    for (int i = 0;i < s.size(); i++) {
        if (s[i] != 'X' && s[i] != 'Y' && s[i] != 'Z'){

        }
    }
    return 0;
}
```

`第四步：flag变量标记`
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    cin >> s;
    int flag = 0;
    for (int i = 0;i < s.size(); i++) {
        if (s[i] != 'X' && s[i] != 'Y' && s[i] != 'Z'){
            flag = 1;
            break;
        }
    }
    return 0;
}
```

`第五步：flag变量判断`
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    cin >> s;
    int flag = 0;
    for (int i = 0;i < s.size(); i++) {
        if (s[i] != 'X' && s[i] != 'Y' && s[i] != 'Z'){
            flag = 1;
            break;
        }
    }
    if (flag == 0) {
        cout << "Pass";
    } else {
        cout << "Not Pass";
    }
    return 0;
}
```


### 举例2：
**题目描述**
输入一个整数n（1≤n≤100），然后输入n个正整数，请输出n个正整数中除了最大值以外的数字，以空格分隔。

**输入描述**
> 输入2行，第1行为一个整数n（1≤n≤100），第2行为n个正整数，以空格分隔。

**输出描述**
> 输出1行，输出n个正整数中除了最大值以外的数字，以空格分隔。

**样例1**
**输入**
```
7
4 8 7 9 2 9 1
```
**输出**
```
4 8 7 2 1
```

**题目分析：**这一题结合了数组的输入、找最大值、if选择等

**解题步骤：**
    1. 输入n个数
    2. 找到最大值
    3. 循环输出，如果是最大值就跳过，否则输出

尝试详细分析并解决这道题吧！

题目链接：[抛弃最大值](https://oj.youdao.com/course/83/521/2#/1/7813)
