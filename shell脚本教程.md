# Shell脚本教程
> 本教程基于标准shell，即bash，全称Bourne Again Shell，是linux默认shell，故被称为标准shell
> 本教程默认读者具备基本的shell命令基础以及编程基础，即在命令行下使用`cd`、`ls`等命令的基础以及至少掌握了一门编程语言的基础使用方法

## 一、第一个shell脚本
1. 使用`touch test.sh`创建test.sh脚本后用编辑器（nano/vi/vim等）编辑，或者直接使用编辑器（以vim为例）编辑，保存后会自动创建
2. 用编辑器打开test.sh文件，输入下面内容：
```shell
#!/bin/bash
echo "hello world"
#输出hello world
```

#### 代码解析：
1. `#!/bin/bash`：声明脚本用bash运行，这一行在某些情况下可以不写（后面会说明）。
2. `echo "hello world"`：输出**hello world**，`echo`是常用的输出函数
3. `#输出hello world`代码注释，`#`后的内容都是注释

#### 运行脚本：
1. 为脚本添加可执行权限，并执行
```shell
λ chmod +x test.sh
λ ./test.sh
```
第一行是给test.sh脚本添加**可执行权限**；第二行是运行脚本，这里一定要`目录+脚本`的形式运行，当前目录则用`./`表示

2. 作为bash解释器的参数
```shell
λ bash test.sh
```
通过这种形式运行的脚本则不需要`#!/bin/bash`来声明解释器

#### 拓展知识
**文件拓展名**，即后缀，此处为`.sh`。其主要作用是让用户可以快速分辨文件的类型，系统识别文件类型通常具有更加雄健的方式，在这里，读者可以进行下面一些尝试：
1. 删除文件拓展名：创建文件`a`，用编辑器打开该文件，将上述代码填入其中，然后分别用上面两种执行方式进行执行
2. 更改文件拓展名：创建文件`a.c`，用编辑器打开该文件，将上述代码填入其中，然后分别用上面两种执行方式进行执行

无论如何设置文件拓展名，最后都会正常输出**hello world**。所以，在写解释型语言（我们通常称为脚本语言）的时候，如果想要写出来的脚本能够直接运行（`./test.sh`）而不是每次都要声明解释器（`bash test.sh`），就需要在第一行声明其需要用的解释器。其他脚本语言如`python`、`lua`、`php`等均如此

## 二、bash变量
### 1. 自定义变量
> 自定义变量是通常意义的变量，由用户自己定义，其使用范围仅在当前shell中

**变量的命名规则如下：**
1. 只包含字母、数字、下划线
2. 不能以数字开头
3. 不能使用bash的关键字

#### 定义变量
`str="hello world"`，定义`str`为**hello world**

<font color="red">注意：等号`=`的左右不能添加空格</font>

#### 使用变量
在变量前添加`$`即可，通常在使用变量的时候会用`""`或者`{}`来隔开变量和其他字符。当然用空格也是可以的，但是在某些情况下，两个变量需要紧紧贴在一起，这时空格就不适用了。具体使用方式如下：
```shell
str="hello world"
echo $str
echo "$str"
echo ${str}
str="你好世界"
echo "输出：${str}"
echo "输出：$\"str\""
echo "输出：${str}${str}"
```

<font color="red">注意：这里不能使用单引号`''`，单引号将内容视为字符串直接输出而不会转义</font>


#### 只读变量
在上面的代码当中，我们对变量进行了一次重新赋值，如果想要确保一个变量的值不被覆盖，可以通过`readonly`将其设置为只读变量（或许可以叫做常量？），这个命令不需要加`$`
```shell
str="hello world"
readonly str
echo $str
str="你好世界"
echo $str
```

输出如下：
```shell
hello world
a.sh: 行 4: str: 只读变量
hello world
```

#### 删除变量
通过`unset`可以删除一个变量，但是不能删除`readonly`所保护的变量，用法同`readonly`，不用加`$`。但是感觉这个的作用不大？通常作用在临时变量或者调试用的变量上，这里不做演示

#### 变量类型
使用`declare`或者`typeset`命令（据说这两个命令是完全一样的）可以给变量增加类型属性
| 选项 | 作用                               |
|------|------------------------------------|
| `-a` | 增加数组属性                       |
| `-A` | 增加关联数组属性                   |
| `-i` | 增加整型属性                       |
| `+i` | 删除整型属性                       |
| `-l` | 增加小写属性，变量的值均转化为小写 |
| `+l` | 删除小写属性                       |
| `-u` | 增加大写属性，变量的值均转化为大写 |
| `+u` | 删除大写属性                       |
| `-n` | 增加引用属性                       |
| `+n` | 删除引用属性                       |
| `-r` | 增加只读属性                       |
| `-t` | 增加追踪属性                       |
| `+t` | 删除追踪属性                       |
| `-x` | 增加导出属性                       |
| `+x` | 删除导出属性                       |
| `-p` | 显示每个名称的属性和值             |
| `-g` | 将变量定义为全局，通常在函数中使用 |

```shell
declare -i a=5
declare -l str="ABC"
declare -u STR="abc"
echo ${a} ${str} ${STR}
```

#### 字符串
> 字符串是shell中较为常用的类型，这里着重讲一下。

##### 1. 字符串的定义
shell中可以通过单引号`''`或者双引号`""`来定义字符串。单引号不支持出现变量和一些转义，所以通常我们会选择双引号。读者可以自行运行下面的脚本查看差别：
```shell
name="KicamonIce"
str1='hello \"KicamonIce\"\n'
str2='hello \"${name}\"\n'
str3="hello \"KicamonIce\"\n"
str4="hello \"${name}\"\n"
echo -e $str1 $str2 $str3 $str4 
```

##### 2. 获取字符串的长度
通过`#`可以获取字符串长度，注意这里需要使用`{}`
```shell
str="hello world"
echo ${#str}
```

##### 3. 字符串拼接
将两个字符串放在一起就能拼接，双引号可以通过变量来拼接，更加灵活
```shell
str="world"
str1='hello''world' #将字符串hello和字符串world拼接在一起
echo $str1
str2="hello${str}"
echo $str2
```

##### 4. 获取子字符串
举例：从第**3**个字符开始，输出两个字符。这里需要注意，下标是从0开始
```shell
str="hello world"
echo ${str:2:2}
```

##### 5. 查找子串
1. 使用`exper index`可以查找子串，这种方式会返回子串中任意字符首次出现的位置。如果没有查找到则输出0
```shell
str="I'am KicamonIce"
echo `expr index "$str" Ki`
echo `expr index "$str" ia`
```

2. 使用`grep`、`head`、`cut`结合管道`|`获取完整子串出现位置，如果没有查找到则输出空值
```shell
str="I'am KicamonIce"
echo $str | grep -bo "Ki" | head -n1 | cut -d: -f1
```

#### 数组
##### 1. 定义数组
shell的数组定义，可以使用`declare -a`命令定义数组,也可以通过下面方式定义数组并初始化。将数组中的元素用`空格`或者`换行`隔开，数组对类型没有限制，可以混用
```shell
array1=(1 2 3 "abc")
array2=(
1
2
3
"abc"
)
```

##### 2. 访问数组元素
通过`${变量名[下标]}`即可访问数组中的元素，下标可以不连续，也可以越界，运行下面代码即可理解
```shell
array=(1 2 3 "abc")
array[10]=666
echo ${array[3]}
echo ${array[6]}
echo ${array[10]}
```

##### 3. 获取全部元素
通过`@`这个下标可以获取数组中的所有变量
```shell
array=(1 2 3 "abc")
echo ${array[@]}
```

##### 4. 数组长度
和字符串类似，使用`#`即可，不过这里需要配合`@`下标使用
```shell
array=(1 2 3 "abc")
echo ${#array[@]}
```

##### 5. 关联数组
数组只能通过数字作为下标来访问元素，而关联数组可以通过字符串或数字来访问。关联数组需要通过`declare -A`来定义
```shell
declare -A table
table["name"]="KicamonIce"
table["age"]=21
table["sex"]="武装直升机"
echo ${table["name"]}
```

#### 特殊变量
在shell中，有一些特殊的变量，主要用于**多脚本**联合以及**函数**使用
| 变量 | 作用                                                        |
|------|-------------------------------------------------------------|
| `$0` | 获取脚本名                                                  |
| `$1` | 第一个参数，其他数字以此类推                                |
| `$#` | 参数数量                                                    |
| `$?` | 上一个命令的退出状态，`0`表示正常退出，其他数字表示执行失败 |
| `$@` | 分别输出全部变量                                            |
| `$*` | 将所有变量拼接为一个字符串并输出                            |
| `$$` | 输出脚本运行的进程ID号                                      |
| `$!` | 后台运行的最后一个ID号                                      |
| `$-` | 显示Shell使用的当前选项，与set命令功能相同                  |

创建脚本`a.sh`
```shell
echo $0
echo $1
echo $2
ehoc $#
echo $?
cccc
echo $?
```

通过下面方式运行`bash a.sh 222 333`，自行查看运行结果即可理解


### 2. 环境变量
> 上述变量只能在当前shell中使用，而环境变量则可以由当前shell及其子shell共同使用。环境变量通常用于shell的配置

环境变量的定义方式为`export`

读者可以进行下面的尝试，创建shell脚本`a.sh`和`b.sh`
1. 使用普通变量：
`a.sh`
```shell
a=10
bash ./b.sh
```

`b.sh`
```shell
echo $a
```

2. 使用环境变量
`a.sh`
```shell
export a=10
bash ./b.sh
```

`b.sh`
```shell
echo $a
```

分别运行之后查看结果，这里`b.sh`是`a.sh`的**子shell**
